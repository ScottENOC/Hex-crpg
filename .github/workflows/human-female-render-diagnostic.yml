name: Human Female Tactical Render Diagnostic

on:
  push:
    branches:
      - ci/equipment-fit-validation

jobs:
  tactical-render:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Run equipment render regressions
        run: npx playwright test tests/human-female-tactical-render.spec.js tests/human-female-directional-art.spec.js --reporter=line
