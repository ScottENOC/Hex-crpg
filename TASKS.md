The eagle image is huge. Shrink it by 50%. Also, the image that displays in the initiative tracker should match whatevers on the map (ie flying or ground image depending on status - it currently shows the default elf picture).

When I was in the Arena lobby, when all my summons disappeared, it then said I'd won a fight and teleported me back to the lobby. This teleport back to the lobby should only be possible from a fight area, not from the lobby. We also don't need to check for it when a friendly dies, only if an enemy dies and there are no enemies left. Also, the text should not refer to this as teleporting. "Heading back to".

The rest until restored button triggers us to rest until fully restored, but then the UI doesn't update to acknowledge this until we take some other action. Can the rest until restored do a ui refresh when complete (i.e. the text that tracks our hp, mana etc, not reloading all the images.

I want to test the fly ability. Can you give me a cheat button that enables all friendly characters to fly (and then the button is replaced with a 'remove flying' button. Obviously, I don't have an image for a flying elf or anything, so no need to change images for flying characters other than eagle

